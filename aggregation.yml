# Runboat OCA CI dependency repos
# Cloned by gitaggregate before tests run
- url: https://${GITHUB_TOKEN}@github.com/bmya/odoo
  branch: "18.0"
  merges:
    - "origin 18.0"
- url: https://${GITHUB_TOKEN}@github.com/bmya/enterprise
  branch: "18.0"
  merges:
    - "origin 18.0"
