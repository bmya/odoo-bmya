- url: https://${GITHUB_TOKEN}@github.com/bmya/odoo
  branch: "master"
  merges:
    - "origin master"
- url: https://${GITHUB_TOKEN}@github.com/bmya/enterprise
  branch: "master"
  merges:
    - "origin master"
- url: https://${GITHUB_TOKEN}@github.com/bmya/design-themes
  branch: "master"
  merges:
    - "origin master"
