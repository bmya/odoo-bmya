name: CI

on:
  pull_request:
    branches: ['16.0', '17.0', '18.0', '19.0', 'master']
  workflow_dispatch:

jobs:
  lint:
    uses: bmya/bmya-ci/ci-lint.yml@main
    with:
      odoo_version: ${{ github.base_ref }}
