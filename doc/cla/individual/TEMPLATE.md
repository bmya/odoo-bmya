<!--
INSTRUCTIONS / INSTRUCCIONES

1. Copy this file to `<your-github-login>.md` (all lowercase). Example: if your
   GitHub login is `JDoe`, the filename must be `jdoe.md`.

   Copia este archivo a `<tu-login-github>.md` (todo en minúsculas). Ejemplo: si
   tu login de GitHub es `JDoe`, el filename debe ser `jdoe.md`.

2. Also copy `TEMPLATE.yaml` to `<your-github-login>.yaml` with the same login
   and fill it with your machine-readable data.

   Copia también `TEMPLATE.yaml` a `<tu-login-github>.yaml` con el mismo login
   y complétalo con tus datos parseables.

3. Replace the placeholders below (everything in <ANGLE_BRACKETS>) with your
   real values, then remove this instruction block.

   Reemplaza los placeholders abajo (todo en <CORCHETES_ANGULARES>) con tus
   datos reales, luego elimina este bloque de instrucciones.

4. Open a pull request on `bmya/odoo-bmya` targeting the `16.0` branch with
   both files. After merge, the BMyA forward-port bot will propagate your
   signature to 17.0, 18.0, etc.

   Abre un pull request en `bmya/odoo-bmya` apuntando a la rama `16.0` con
   ambos archivos. Después del merge, el forward-port bot de BMyA propagará
   tu firma a 17.0, 18.0, etc.
-->

<COUNTRY>, <YYYY-MM-DD>

I hereby agree to the terms of the BMyA Individual Contributor License
Agreement v1.0 ([icla-1.0.md](../icla-1.0.md)).

I declare that I am authorized and able to make this agreement and sign this
declaration.

Signed,

<FULL NAME> <git_email> https://github.com/<your-github-login>

---

Por la presente acepto los términos del Acuerdo de Licencia de Colaborador
Individual de BMyA v1.0 ([icla-1.0.md](../icla-1.0.md)).

Declaro que estoy autorizado/a y en condiciones de celebrar este acuerdo y
firmar esta declaración.

Firmado,

<NOMBRE COMPLETO> <git_email> https://github.com/<tu-login-github>
